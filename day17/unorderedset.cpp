#include<bits/stdc++.h>
using namespace std;

void explainUnorderedSet() {//unique but not ordered
   unordered_set <int> ust;// lower bound and upper_bound doesnt work but all functions are samee
    //time complexity of o(1) but in worst case it can reach o(n)
}

int main(){
    explainUnorderedSet();
}