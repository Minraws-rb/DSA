#include<bits/stdc++.h>
using namespace std;

void explainMultiMapandUnorderedMap() {
    // Multimap is same as map except one key can store multple values// ie duplicate keys and ordered
   // cannot be used as cout << mpp[2]

   // Unordered map is unique keys but not ordered
   //o(1) and in worst case o(n)
}
int main(){
    explainMultiMapandUnorderedMap();
}